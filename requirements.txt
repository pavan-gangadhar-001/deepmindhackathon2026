# Prody — merged engine + backend + pentest dependencies.
# Install:  pip install -r requirements.txt

# --- LLM / agents (Gemini Managed Agents / Interactions API) ---
google-genai>=2.11.0

# --- Backend API + SSE (engine :8000, pentest :8900) ---
fastapi
uvicorn[standard]
pydantic>=2
python-multipart
python-dotenv

# --- HTTP client (gate calls, SRE health checks) + parsing ---
httpx
beautifulsoup4
